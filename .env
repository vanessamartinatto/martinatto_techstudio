DATABASE_URL=file:/home/z/my-project/db/custom.db

# --- Contact form email (Task 4/29) ---
# Recipient of the form notifications:
CONTACT_EMAIL=info@martinatto.it
# Sender account (fill SMTP_USER + SMTP_PASS to activate; defaults: smtp.gmail.com:465)
SMTP_HOST=smtp.ionos.it
SMTP_PORT=465
SMTP_USER=info@martinatto.it
# mailbox password (IONOS webmail password) - pending, user will provide:
#SMTP_PASS=
